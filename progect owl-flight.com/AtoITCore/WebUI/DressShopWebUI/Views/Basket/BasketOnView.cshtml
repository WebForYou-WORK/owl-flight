@model Domain.Entityes.Basket
    @if (Model.CountItem != 0)
    {
    <div id='follow'>
        <p style="margin-bottom: -10px"><b>@Model.CountItem</b></p>
        <a href="@Url.Action("Index", new {returnUrl = Request.Url?.PathAndQuery})" class="basketLink">
            <img src='~/img/basketpic.png' alt='Ваш кошик' title='Ваш кошик' style="width: 50px; height: 50px" />
        </a>
    </div>
    }
